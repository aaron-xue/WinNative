diff --git a/src/freedreno/common/freedreno_dev_info.h b/src/freedreno/common/freedreno_dev_info.h
index 3a6eeed..9b39d4c 100644
--- a/src/freedreno/common/freedreno_dev_info.h
+++ b/src/freedreno/common/freedreno_dev_info.h
@@ -480,6 +480,8 @@ struct fd_dev_info {
 
       /* Whether the device supports the image processing opcode */
       bool has_image_processing;
+      /* If GMEM needs to be disabled for this GPU */
+      bool disable_gmem;
 
       /* The amount of valid draw state IDs. */
       uint32_t max_draw_states;
diff --git a/src/freedreno/common/freedreno_devices.py b/src/freedreno/common/freedreno_devices.py
index 47791d6..9b93ceb 100644
--- a/src/freedreno/common/freedreno_devices.py
+++ b/src/freedreno/common/freedreno_devices.py
@@ -842,6 +842,8 @@ a7xx_gen1 = GPUProps(
         enable_tp_ubwc_flag_hint = True,
         reading_shading_rate_requires_smask_quirk = True,
         cs_lock_unlock_quirk = True,
+        has_early_preamble = False,
+        has_scalar_predicates = False,
     )
 
 a7xx_gen2 = GPUProps(
@@ -1104,7 +1106,7 @@ add_gpus([
         GPUId(chip_id=0xffff43050c01, name="Adreno X1-85"),
     ], A6xxGPUInfo(
         CHIP.A7XX,
-        [a7xx_base, a7xx_gen2],
+        [a7xx_base, a7xx_gen2, GPUProps(enable_tp_ubwc_flag_hint = True)],
         num_ccu = 6,
         tile_align_w = 96,
         tile_align_h = 32,
@@ -1355,6 +1357,7 @@ a8xx_gen2 = GPUProps(
 )
 
 add_gpus([
+        GPUId(chip_id=0x44010000, name="Adreno (TM) 810"), # KGSL
        GPUId(chip_id=0xffff44010000, name="Adreno (TM) 810"),
     ], A6xxGPUInfo(
         CHIP.A8XX,
@@ -1365,6 +1368,7 @@ add_gpus([
             # This is possibly also needed for a830 (and all of a8xx),
             # move to a8xx_base if confirmed needed for a830.
             has_fs_tex_prefetch = False,
+            disable_gmem = True,
         )],
         num_ccu = 1,
         num_slices = 1,
@@ -1380,6 +1384,28 @@ add_gpus([
         raw_magic_regs = a8xx_base_raw_magic_regs,
     ))
 
+add_gpus([
+        GPUId(chip_id=0x44030000, name="Adreno (TM) 825"),
+    ], A6xxGPUInfo(
+        CHIP.A8XX,
+        [a7xx_base, a7xx_gen3, a8xx_base, a8xx_gen1, GPUProps(
+            sysmem_ccu_depth_cache_fraction = CCUColorCacheFraction.THREE_QUARTER.value,
+            sysmem_per_ccu_depth_cache_size = 96 * 1024,
+        )],
+        num_ccu = 4,
+        num_slices = 2,
+        tile_align_w = 96,
+        tile_align_h = 32,
+        tile_max_w = 16416,
+        tile_max_h = 16384,
+        num_vsc_pipes = 32,
+        cs_shared_mem_size = 32 * 1024,
+        wave_granularity = 2,
+        fibers_per_sp = 128 * 2 * 16,
+        magic_regs = dict(),
+        raw_magic_regs = a8xx_base_raw_magic_regs,
+    ))
+
 add_gpus([
         GPUId(chip_id=0xffff44050000, name="Adreno (TM) 830"),
         GPUId(chip_id=0x44050001, name="Adreno (TM) 830"), # KGSL
@@ -1401,6 +1427,8 @@ add_gpus([
     ))
 
 add_gpus([
+        GPUId(chip_id=0x44030A00, name="Adreno (TM) 829"), # KGSL
+        GPUId(chip_id=0xffff44030A00, name="Adreno (TM) 829"), # KGSL fallback
         GPUId(chip_id=0x44030a20, name="Adreno (TM) 829"), # KGSL
     ], A6xxGPUInfo(
         CHIP.A8XX,
@@ -1408,6 +1436,7 @@ add_gpus([
          GPUProps(
             shading_rate_matches_vk = True,  # TODO confirm this
             sysmem_vpc_bv_pos_buf_size = 24576,
+            disable_gmem = True,
          )],
         num_ccu = 4,
         num_slices = 2,
diff --git a/src/freedreno/vulkan/tu_cmd_buffer.cc b/src/freedreno/vulkan/tu_cmd_buffer.cc
index 6cf3050..a29d913 100644
--- a/src/freedreno/vulkan/tu_cmd_buffer.cc
+++ b/src/freedreno/vulkan/tu_cmd_buffer.cc
@@ -1371,6 +1371,11 @@ use_sysmem_rendering(struct tu_cmd_buffer *cmd,
       return true;
    }
 
+   if (cmd->device->physical_device->info->props.disable_gmem) {
+      cmd->state.rp.gmem_disable_reason = "GMEM disabled for this GPU";
+      return true;
+   }
+
    /* can't fit attachments into gmem */
    if (!cmd->state.tiling->possible) {
       cmd->state.rp.gmem_disable_reason = "Can't fit attachments into gmem";
